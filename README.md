# How to insert database
1. Remove all data from your db
   (You can use 'truncate table table_name cascade;' query to clear data. The difference between this query and delete query is it will reset the auto-increment counters.)
3. Run the main method from Main class
